// wait.js
window.addEventListener('DOMContentLoaded', () => {
    console.log("Environment initialized: Shell is ready.");

    // Optional: add a class to the body to trigger CSS transitions once loaded
    document.body.classList.add('app-loaded');
});
